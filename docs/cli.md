# Command Line Usage

Run all mini apps:

python -m src.cli

Filter by status:

python -m src.cli --status Testing

Filter by category:

python -m src.cli --category Game

Search by keyword:

python -m src.cli --search Startale

Filter by tag:

python -m src.cli --tag miniapp
